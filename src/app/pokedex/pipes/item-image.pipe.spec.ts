import { ItemImagePipe } from './item-image.pipe';

describe('ItemImagePipe', () => {
  it('create an instance', () => {
    const pipe = new ItemImagePipe();
    expect(pipe).toBeTruthy();
  });
});
